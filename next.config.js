/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  images: {
    domains: [
      "images.bewakoof.com",
      "images.unsplash.com",
      "m.media-amazon.com",
      "avatars.githubusercontent.com",
    ],
  },
};
